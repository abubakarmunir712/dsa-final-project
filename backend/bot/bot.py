from backend.classes.player import Player
from backend.classes.deck import Deck
from backend.classes.card import Card


class Bot(Player):
    def __init__(self, cards, name="Unknown", is_AI=True, player_id=None):
        super().__init__(cards, name, is_AI, player_id)

    def play_yourself():
        pass
     